# Design

Os arquivos em `archives/` são estudos históricos de identidade visual e não são carregados pela aplicação.

Os ativos oficiais em uso ficam em `frontend/public/brand/`. Novos conceitos devem receber nomes descritivos antes de serem versionados.
